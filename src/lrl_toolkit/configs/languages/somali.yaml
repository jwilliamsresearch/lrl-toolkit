display_name: Somali
iso639_3: som
iso639_1: so
nllb_code: som_Latn
scripts:
- Latin
default_script: Latin
direction: ltr
dialects: []
normalization:
  unicode_form: NFC
  normalize_digits: false
  rules: []
sources:
- connector: wikipedia
  params:
    wiki: so
    license: CC-BY-SA-4.0
    url: https://so.wikipedia.org
- connector: glot500
  params:
    glot: som_Latn
    license: mixed
- connector: culturax
  params:
    lang: so
    license: ODC-BY-1.0
- connector: madlad400
  params:
    lang: so
    variant: clean
    license: CC-BY-4.0
- connector: oscar
  params:
    lang: so
    license: CC0-1.0
  notes: >
    OSCAR-2301 access is currently SUSPENDED by OSCAR's maintainers (checked
    2026-07-21), independent of HF_TOKEN. Also, CC0-1.0 covers only the
    dataset's metadata/packaging, not the underlying crawled text, which OSCAR
    explicitly disclaims holding copyright over. Do not add to ingest.sources
    until access is restored and the text-license caveat is reviewed.
- connector: opus
  params:
    src: so
    tgt: en
    corpora:
    - OpenSubtitles
    - Tatoeba
    - NLLB
    license: mixed

instruction_sources:
- repo: CohereForAI/aya_dataset
  instruction_field: inputs
  response_field: targets
  select_field: language_code
  select_value: som
  limit: 5000
  notes: Aya human-written instructions (Apache-2.0), filtered to som.
